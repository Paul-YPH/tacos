from setuptools import setup, find_packages

setup(
    name="tacos",
    version="0.0.1",
    description="/",
    author="/",
    packages=find_packages(),
    install_requires=[
    ],
    python_requires=">=3.7",
)